# Application
spring.application.name=habit-tracker

# Server
server.port=${PORT:7070}

# Database - Railway MySQL (used in production on Render)
spring.datasource.url=${MYSQL_URL:jdbc:mysql://zephyr.proxy.rlwy.net:55565/railway?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
spring.datasource.username=${MYSQLUSER:root}
spring.datasource.password=${MYSQLPASSWORD:nTqvfBiylNgYVlWtfEOglcIbfhCCFNzZ}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT
app.jwt.secret=${JWT_SECRET:habit-tracker-secret-key-that-is-at-least-256-bits-long-for-hs256-algorithm}
app.jwt.expiration=86400000

# CORS
app.cors.allowed-origins=${CORS_ORIGINS:http://localhost:5173,http://localhost:3000}
